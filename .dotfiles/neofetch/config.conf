CRT=$'\e[38;2;194;246;255m'
BLUE=$'\e[38;2;109;159;209m'
WHITE=$'\e[38;2;183;212;232m'
RESET=$'\e[0m'

print_info() {
    prin "${WHITE}${USER}${WHITE}@${HOSTNAME%%.*}${RESET}"
    prin "$(color 14)────────────────"

    prin "$(color 14)╭──────────── ${WHITE}Software$(color 14) ────────────"

    prin "$(color 14)│ ${WHITE}Besturingssysteem" \
        "${WHITE}NixOS 26.05 (Yarara) x86_64${RESET}"

    info "$(color 14)│ ${WHITE}Kernel" kernel

    prin "$(color 14)│ ${WHITE}Pakketten" \
        "${WHITE}$(nix-store --query --requisites /run/current-system | wc -l) (nixpkgs)${RESET}"

    info "$(color 14)│ ${WHITE}Shell" shell
    info "$(color 14)│ ${WHITE}Bureaubladomgeving" de
    info "$(color 14)│ ${WHITE}Terminal" term

    prin "$(color 14)├──────────── ${WHITE}Hardware$(color 14) ────────────"

    info "$(color 14)│ ${WHITE}CPU" cpu
    prin "$(color 14)│ ${WHITE}GPU" \ "Intel DG2 [Arc A770]"
    info "$(color 14)│ ${WHITE}Geheugen" memory

    prin "$(color 14)├──────────── ${WHITE}Actieve Tijd$(color 14) ────────"

    info "$(color 14)│" uptime

    prin "$(color 14)╰──────────────────────────────────"

    info cols
}

title_fqdn="off"
kernel_shorthand="on"
distro_shorthand="on"
os_arch="off"

uptime_shorthand="off"

memory_percent="off"
memory_unit="mib"

package_managers=(nix-system nix-env)

shell_path="off"
shell_version="on"

cpu_brand="off"
cpu_speed="on"
cpu_cores="logical"
cpu_temp="off"


refresh_rate="on"

gtk_shorthand="on"
gtk2="on"
gtk3="on"

public_ip_host="http://ident.me"
public_ip_timeout=2

de_version="off"

disk_subtitle="dir"
disk_percent="on"

music_player="auto"
song_format="%artist% - %title%"
mpc_args=()

colors=(distro)

underline_enabled="off"

separator="›"

color_blocks="on"
block_width=3
block_height=1
col_offset="auto"

bar_char_elapsed="-"
bar_char_total="="
bar_border="on"
bar_length=15

bar_color_elapsed="distro"
bar_color_total="distro"

cpu_display="off"
memory_display="off"
battery_display="off"
disk_display="off"

image_source="auto"
ascii_distro="auto"
ascii_bold="off"

image_loop="off"

thumbnail_dir="${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/neofetch"

crop_mode="normal"
crop_offset="center"

image_size="auto"

gap=3
yoffset=0
xoffset=0

background_color=
stdout="off"
